// Mejores prácticas para declarar nombres de variables

//Notación de altas y bajas (camello):
let nombreCompleto = "Juan Pérez";
console.log(nombreCompleto);

let x,y;
x=10; y=20;
let z=x+y;
console.log(z);


/* ##REGLAS PARA DEFINIR VARIABLES EN JAVASCRIPT
JS es sensible a mayúsculas y minusculas, entonces se tiene que escribir exactamente igual
las variables cuando las utilicemos.

--El nombre de una variable no puede iniciar con un número.
--Se oueden utilizar letras para inicializar una variable, guion bajo, o signo de dólar.
        variable       Variable    _variable       $variable
--No se pueden usar palabras reservadas como variables en JS, ejemplos:
    -abstract   -arguments  -await  -boolean    -break  -byte   -case
    -catch  -const  -continue   -delete     -do     -else       -int
*/
let variable;
let _variable;
let $variable;
