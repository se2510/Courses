/* Un arreglo en JS es un tipo objetc que permite almacenar 
varios valores
*/
// sintaxis vieja de como se declaraban arrays (ya no recomendable)
//let autos = new Array('BMW','Mercedes Benz','Volvo');

// Forma recomendable de declarar arreglos:
const autos = ['BMW','Mercedes Benz','Volvo']; 
// se hizo con const porque la variable de memoria no se va a modificar
// aunque si se puede modificar el contenido de la variable
console.log(autos);