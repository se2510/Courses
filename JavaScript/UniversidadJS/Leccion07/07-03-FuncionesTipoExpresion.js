// Opciones para definir y mandar a llamar funciones en JS
// Es util cuando se combina JS y HTML


// Declaracion de una funcion de tipo expresión
let x = function (a,b){return a+b};

resultado = x(1,2);
console.log(resultado);